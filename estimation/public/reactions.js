// The limited set of emoji reactions, shared by the client (to build the
// palette) and the server (to validate incoming reactions). Single source of
// truth so the two never drift.
export const REACTIONS = ['😂', '😮', '😡', '😈', '😭', '👍', '🎲', '🤥'];
